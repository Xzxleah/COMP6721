the submitted files includes:

code: the data-processing, training and evaluation code in .ipynb format；
      The training code could be run by modify the file path to the dataset

dataset: The entire mask image dataset which includes five folder mapping to five mask image categories

trained_model: include saved trained model generated by the training code,The testing dataset with according five mask categories and The test code whose name test.ipynb
	       The test code can run directly by using juptyer notebook,
	       The test code will provide the best testing performance of our trained model and The evaluation performance can be immediately seen after running the code.

comp_6721_project_part1_OB3.pdf: The project report of project partI
